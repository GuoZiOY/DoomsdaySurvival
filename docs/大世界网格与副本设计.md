# 大世界网格化 + 区域副本化 · 设计文档

> 版本目标：**v51** ｜ 前置：v50（探索三层全通 + 清奇幻「地图」遗留，`7535b4e`）
> 本文是施工图：口径 → 复用盘点 → 技术冲突与解法 → 数据设计 → 分刀计划 → 验证方案。
> 纪律：每一刀都「先 `Tools/编译校验.ps1` → 再离线验证器 → 再试玩」，数值与文案不在重构刀里顺手动。

---

## 〇、拍板口径（已定，2026-09-12）

| # | 问题 | 定版 |
|---|---|---|
| 1 | 大世界表现形式 | **格子网格 100×100**（不再用节点网图） |
| 2 | 走一格的成本 | **5 游戏分钟/格**（楼里/区域里仍是 1 分钟/格）；表现 0.30 秒/格 |
| 3 | 一格代表什么 | 一片街区/空地。**两套尺度并存**：大世界一格 ≈ 一条街，走进区域后换成区域自己的 32×22 格 |
| 4 | 区域在大世界上的占地 | **多格，形状掩码**（直接复用 `建筑外形` 的 矩形/L/凸/凹 + 朝向/镜像） |
| 5 | 一个区域副本里是什么 | **一片街区** —— 复用现有 `区域网格`（32×22 地块制摆 4 栋楼 + 街道障碍 + 区域迷雾）→ 建筑（楼层）→ 房间 |
| 6 | 「种子固定」的作用域 | **每存档一个 `世界种子`**（沿用 `玩家档案.户型种子` 的做法）；区域布局种子 = `派生(世界种子, 区域标识)` |
| 7 | 副本「可重复刷」的刷新 | **每副本独立重置计时**（游戏小时，数据可配）；**布局种子永不变** |
| 8 | 大世界搜索 | **有代价的一次性建筑**：消耗行动点 + 游戏分钟 → 半径内生成 0~1 个临时建筑 → 进去探索，**一次性、不刷新** |
| 9 | 100×100 的渲染 | **视口虚拟化 + 大世界免建底格/格线** |

---

## 一、目标结构

```
【现在】大世界（节点网图，map.json 不存在 → 空字典，纯占位）
           ↓ 打开野外面板事件（快速测试面板 直接调 进入区域(标识, 随机种子)，绕过 地图服务）
        区域网格 32×22 ──→ 建筑（楼层 + 楼梯间）──→ 房间
        （区域 = 一屏网格，每次进去随机种子）

【目标】
        大世界 · 100×100 格子网格（令牌 / A* / 逐格推进 / 三态迷雾 / 相机跟随 —— 全部继承 格子探索服务）
          ├─ 区域副本（多格形状掩码占地，如 6×5）
          │     · 布局种子固定：派生(世界种子, 区域标识) → 同一存档永远同一套楼
          │     · 解锁条件（未解锁 → 地图上可不见 / 或可见但进不去 + 提示）
          │     · 独立重置计时：布局不变，内容（容器/敌人/尸体/门锁）按计时刷新
          │     ↓ 走到占地任意一格
          │   区域网格 32×22（原样复用）──→ 建筑（楼层）──→ 房间（原样复用）
          ├─ 临时建筑（大世界「搜索」在半径内生成，一次性，不刷新）
          │     ↓ 走上去
          │   建筑 / 房间（复用同一套）
          └─ 营地 = 安全屋面板（进屋 = 现有 返回营地 那条路）
```

**结构层级从「四层」变成「五层」**，但**只有最外面一层是新的**：

```
大世界（新）→ 区域（已有）→ 建筑（已有）→ 房间（已有）
```

---

## 二、复用盘点（哪些白送、哪些要新写）

| 组件 | 现状 | 大世界怎么用 |
|---|---|---|
| `格子探索服务`（Services） | 令牌 / A* / 逐格推进 / 三态迷雾 / 视野记忆 / 落点 / 回安全屋清战局 / 7 个钩子 | ✅ **白送**。`大世界探索服务 : 格子探索服务` 只填 4 个钩子 + 一个 `移动游戏分钟` 覆写（5 分钟） |
| `网格寻路 / 网格视野 / 视野规则 / 确定性随机` | 通用 | ✅ 白送 |
| `建筑外形`（形状掩码） | 矩形/L/凸/凹 + 朝向/镜像 → `bool[] 掩码` | ✅ **复用为区域占地**（`建筑外形.生成` 返回的掩码直接当区域占格用） |
| `区域生成器` | 地块制摆楼 + 街道障碍 + 校验 | ✅ 原样用（副本内部） |
| `建筑生成器` / `房间生成器` | 楼层 + 楼梯 / 中空大房间 | ✅ 原样用 |
| `区域探索服务 / 房间探索服务` | 区域层 / 房间层语义 | ✅ 基本不动；只需让「进楼」这条路在副本里也通 |
| `搜索服务` | 容器战局数据 / 尸体容器 / 已翻标记 | ✅ 复用；**新增一个「临时建筑」来源** |
| `世界时间管理器` | 推进 / 跳时 / 腐坏 / 生长 / 净化 | ✅ 复用；**新增「副本重置计时」推进**（与净化同模式：推进+跳时两流） |
| `探索图层`（UI） | 逐格建图（地表/迷雾/交互）+ 相机 + 令牌走路 | ⚠ **要加"格子池"虚拟化**（见 §3.2） |
| `网格面板基类` | 逐格建底格（W×H）+ 逐格建格线（(W+1)×H + (H+1)×W） | ⚠ **要加"要不要底格/格线"开关**（大世界关掉，省 30,200 张图） |
| `探索网格面板` | 实体框骨架 / 点格反馈 / 菜单 | ✅ 复用，大世界只填"画什么/菜单给什么" |
| `地图服务` / `地图地点` / `map.json` | **无数据（map.json 不存在）** | ❌ **整块退役**，由 大世界探索服务 接管 |
| `地图模式` | `{ 大地图, 野外 }` | 保留概念，改语义为 `{ 大世界, 区域, 房间 }`（HUD 地点位用） |

**结论**：新写的只有 **大世界生成器（Domain）+ 大世界探索服务（Service）+ 大世界面板/图层/贴图（UI）+ 图层虚拟化 + 副本重置计时 + 搜索生成临时建筑**。其余全是接线。

---

## 三、关键技术冲突与解法

### 3.1 ★ 副本「可重复刷」 vs 「回安全屋才清」

**冲突**：v48/v49 定的规矩是（写死在 `格子探索服务.cs:42-45, 90-96`）：

```
容器战局数据（已生成内容 / 已翻标记 / 尸体容器）：进房间、出房间都不清 → **回安全屋才清**
视野记忆  视野记忆[种子#模板]：换房/出楼都存、进房装回 → **回安全屋才清**
门锁态    已解锁门[当前种子#门标识]：走回头路记得 → **回安全屋才清**
楼层缓存  Dictionary<"层#序号", 网格数据>：进新楼/出楼/回安全屋清
```

这四样都是「**一趟出行的临时数据**」，而副本要能反复刷 = 这四样要能**按副本**重置。

#### 解法：**两把钥匙**

```
布局种子（永不变）  = 派生(世界种子, 区域标识)          ← 只喂 区域生成器 → 布局永远一样
战局代次（重置时+1）  = 玩家档案.副本状态[区域标识].代次   ← 只喂"内容键"
```

于是所有「内容键」都带上代次，**代次一变，旧内容自动失联**，不需要按前缀枚举清理：

| 键 | 现在 | 改成 |
|---|---|---|
| 容器实例标识 | `{当前种子}#{实体标识}` | `{当前种子}#{实体标识}#g{代次}` |
| 门锁态 | `{当前种子}#{门标识}` | `{当前种子}#{门标识}#g{代次}` |
| 楼层缓存 | `"{层}#{序号}"` | `"{层}#{序号}#g{代次}"` |
| 视野记忆 | `{种子}#{模板}` | **不带代次**（布局没变，看过的街景一直记得 —— 这是好事） |

> **为什么代次不进布局种子**：进了的话每次重置布局就变，直接违背"种子固定"。所以**布局与内容必须用两把钥匙**。
> 这与 v49「楼层进种子 → 三套状态自动分层」（`docs/重构-格子探索基类设计.md` §13.3）是同一个手法。

#### 重置计时

- 数据：`区域模板.重置小时`（游戏小时，可配；0 = 永不重置，一次性副本）
- 状态：`玩家档案.副本状态: List<副本状态>` `{ 区域标识, 剩余重置分钟, 代次 }`（**List 不用 Dictionary** —— `JsonUtility` 不支持字典，项目现有做法就是 List，如 `已清空地点` / `已习得配方`）
- 推进：**逐帧递减**，走 `世界时间管理器` 的 `推进()` + `跳时()` 两条流（与 生长/净化 同模式，见 `工作移交报告.md` 开发注意事项 #4 —— **不要放进 `结算时间段`，会与逐帧双扣**）
- 归零：`代次 += 1` → `剩余重置分钟 = 重置小时 × 60` → 播报一条日志（"你听说那片地方又有人进去了……"之类）
- **不需要主动清数据**：代次一变，旧键再也取不到；垃圾在**回安全屋**时随现有 `清战局()` 一起清

#### 谁负责重置

新增 `副本管理器`（Service，或并入 大世界探索服务）：
```
推进副本计时(分钟)   ← 世界时间管理器 推进/跳时 调
代次(区域标识)       ← 区域探索服务 / 房间探索服务 取
副本键(区域标识, 实体标识)  ← 拼 "{种子}#{实体}#g{代次}"
```

`格子探索服务.实例标识` / `记忆键` 已经是 `protected virtual` → 派生类覆写即可，**基类一行不用改**。

---

### 3.2 ★ 100×100 的渲染：视口虚拟化

**证据**（逐格建图，与格数成正比）：

| 层 | 建法 | 32×22 | **100×100** |
|---|---|---|---|
| 底座层 底格（`网格面板基类.渲染.cs:59-61`） | 每格 1 张 | 704 | **10,000** |
| 线层 分隔线（同文件 `:183-196`） | `(列+1)×行 + (行+1)×列` | 1,462 | **20,200** |
| 地表层（`探索图层.cs:117-129`） | 每格 1 张 | 704 | **10,000** |
| 迷雾层（同文件 `:131-137`） | 每格 1 张 | 704 | **10,000** |
| 交互层（同文件 `:139-148`） | 每格 1 张 + 1 个 `探索格点击` | 704 + 704 组件 | **10,000 + 10,000 组件** |
| **合计** | | **≈4,278 张** | **≈60,200 张 + 10,000 组件** |

#### 解法 A：大世界免建底格 / 格线（省 30,200 张）

地表层本来就**逐格盖满**，底座层那 10,000 张底格与线层那 20,200 条格线在大世界上**完全看不见**。

给 `网格面板基类`（或 `探索网格面板`）加一个开关：
```csharp
// 只**新增**字段；不动任何 [SerializeField]，派生类原字段原样留着（红线 #1）
protected virtual bool 建底格 => true;      // 大世界覆写 false
protected virtual bool 建格线 => true;      // 大世界覆写 false
```
`刷新网格()` 里那两处循环按开关跳过。**对房间层/区域层零影响**（默认 true）。

#### 解法 B：地表 / 迷雾 / 交互 三层改「格子池」（省到 ≈1,400 张）

窗口 1720×960 → 可见约 20×12 格；**只实例化窗口 + 1 屏边距**的格子（约 26×18 = 468 格）：

```
格子池[i] = { Image 图, int 格列, int 格行 }     // i = 池内下标，与"格坐标"解耦
每帧（相机移动超过 1 格 或 换了世界）：
    算出窗口覆盖的格子区间 [列0..列1] × [行0..行1]
    对区间里每个格：从池里取一张图 → 摆到该格 → 按该格状态重画；用不到的图 enabled = false
```

要点：
- `探索图层` 已经有 `视口`（RectMask2D）/ `在窗口内()` / `跟随相机()` / `相机位置` 这套骨架 → **虚拟化是顺着它加，不是推翻**
- **`在窗口内()` 的点击防线照旧保留**（RectMask2D 只裁画面不裁 raycast，见 开发注意事项 #19）
- 池容量要**动态下限**：网格比窗口小（房间层 16×10）→ 池 = 全格（退化成现在的行为，**房间层零变化**）；网格比窗口大 → 池 = 窗口 + 边距
- 令牌走路的 `视觉位置` 与格子池无关（令牌是物品层的一个实体框），不受影响
- 迷雾 / 地表 / 交互三层共用一个「盖住窗口」的格子区间 → 一次算、三层用

**收益**：大世界 ≈ 1,400 张图（比现在的区域层还轻）；**副作用是好的** —— 以后区域层想做更大的片区、房间层想做超大楼，都不用再改。

---

### 3.3 种子派生链（一条线管到底）

```
玩家档案.世界种子                       ← 新游戏随机（沿用 户型种子 的做法，读档恢复）
   └─ 区域布局种子 = 派生(世界种子, "区域:" + 区域标识)          ← 永远固定
        └─ 楼种子   = 派生(区域布局种子, 楼标识)                  ← 现有 区域探索服务.进楼 已在做
             └─ 层种子   = 派生(建筑种子, "F{层}")                 ← 现有 建筑生成器
                  └─ 房间种子 = 派生(建筑种子, "F{层}#{序号}")      ← 现有 建筑生成器
   └─ 临时建筑种子 = 派生(世界种子, "临时:" + 序号) 或 当场随机      ← 一次性，无需固定

战局代次 = 玩家档案.副本状态[区域标识].代次                        ← 只进内容键（§3.1）
```

**注意**：`区域探索服务.进入区域(标识, 种子)` 现在是**外面传种子**（快速测试面板 传随机、面板管理器 传 `Random.Range(1,999999)`）。
改造后**由 大世界探索服务 传 `派生(世界种子, 区域标识)`**，`面板管理器` 那条 `Random.Range` 必须删掉 —— 否则同一片区域每次进去都不一样，副本就无从谈起。

---

### 3.4 大世界搜索 → 临时建筑

```
玩家在 大世界 点「搜索」（HUD 按钮 / 侧边栏 / 大世界面板上的按钮）
   ↓ 前置校验：行动点 ≥ N、不超重？（口径见下）
   ↓ 扣 行动点 + 推进游戏分钟（走 世界时间管理器）
   ↓ 半径 R 内找**可放置的空格**（扫半径内的空格，排除：已有区域占地 / 已有临时建筑 / 障碍 / 界外）
   ↓ 按概率决定"这次有没有"（0~1 个）→ 有则掷一个空位落地
临时建筑实体（网格实体类型.建筑，占地 = 小掩码 1~2 格）+ 一次性种子
   ↓ 走上去 → 复用 区域探索服务.进楼 那条路（建筑模板优先，退化成一个房间模板）
   ↓ 探索完 / 离开 → 实体消失（"一次性、不刷新"）
```

**待定的调参数**（写进 `世界.json`，不写死代码）：

| 参数 | 建议初值 | 说明 |
|---|---|---|
| `搜索行动点` | 10 | 与房间「首次搜索容器」的 10 行动点对齐（v48 定的） |
| `搜索游戏分钟` | 30 | 现实里约 1 秒（30 游戏分 ÷ 30） |
| `搜索半径` | 4 格 | "角色半径内" |
| `搜索出现概率` | 60% | 失败也给反馈（"翻了一圈，什么也没有"） |
| `临时建筑保留` | 一次性 | 进去出来就消失（可在数据里改成保留 N 分钟） |

**与固定副本的定位差异**：临时建筑给**普通资源**（现有 `搜索_地图类型` 的常规容器池）；固定区域副本给**稀有资源**（靠 `区域模板` 指到更高级的 `建筑模板` / 容器池）。这条差异靠**数据**表达，不靠代码分支。

---

### 3.5 时间链（5 分钟/格）

`格子探索服务.移动游戏分钟` 现在是 `const int = 1`（`格子探索服务.cs:19`）。大世界要 5 分钟/格。

**改法**：把 `const` 改成 `protected virtual int 移动游戏分钟 => 1;`（**只新增虚属性，不动现有常量引用点**），大世界覆写为 5。
> ⚠ 红线：`const` 改 `virtual` 会让 `推进一步()` 里的引用从编译期内联变成虚调用 —— 行为不变，但要确认全仓库没有别处用 `格子探索服务.移动游戏分钟` 做编译期常量（如 `const` 参与 switch / 特性参数）。

---

## 四、数据设计

### 4.1 新增 `Assets/Resources/Data/世界.json`

```jsonc
{
  "世界": [
    {
      "标识": "废城",
      "名称": "废城",
      "描述": "……",
      "列": 100,
      "行": 100,
      "营地": { "列": 50, "行": 50 },          // 安全屋/起点
      "区域": [                                // 坐落在大世界网格上的区域副本
        {
          "区域模板": "警察局区",               // 指向 区域模板.json
          "形状": "矩形", "朝向": 0, "左右镜像": false, "占列": 6, "占行": 5,
          "列": 20, "行": 12,                  // 占地左上角在大世界上的坐标
          "解锁": [
            { "条件": "情报", "标识": "警察局位置", "提示": "你还不知道警察局在哪儿。" }
          ]
        }
      ],
      "障碍数": 120,                           // 街道障碍（废墟/车障），撒完校验连通
      "搜索": { "行动点": 10, "游戏分钟": 30, "半径": 4, "出现概率": 60 }
    }
  ]
}
```

**解锁条件**（`条件` 字段，全部满足才解锁）：

| 条件 | 标识含义 | 现有数据支撑 |
|---|---|---|
| `情报` | 需要持有某条情报 | `情报.json` 已存在（收音机池） |
| `物品` | 需要持有某物品 | `玩家档案.持有物品()` |
| `天数` | 需要活到第 N 天 | `玩家档案.游戏天数` |
| `前置区域` | 需要先通关另一片区域 | `玩家档案.已清空()` |

### 4.2 `区域模板.json` 增字段

```jsonc
{
  "标识": "警察局区",
  "名称": "警察局区",
  "描述": "……",
  "危险度": 4,
  "列": 32, "行": 22, "入口边": "下",
  "建筑": [ { "建筑模板": "警察局", "数量": 1 }, { "建筑模板": "西区公寓A", "数量": 2 } ],
  "街道障碍数": 3,
  "战斗棋盘": "街头",
  "重置小时": 24,          // 新增：内容重置周期（0 = 永不重置）
  "稀有度": 3              // 新增：资源档（决定容器池/掉落档；与临时建筑区分）
}
```

### 4.3 `玩家档案` 增字段

```csharp
public int 世界种子;                                   // 新游戏随机；读档恢复（沿用 户型种子 写法）
public List<副本状态> 副本状态 = new List<副本状态>();   // { 区域标识, 剩余重置分钟, 代次 }
public List<string> 已解锁区域 = new List<string>();      // 解锁条件达成后写入
public List<string> 已发现情报 = new List<string>();      // 若 情报 目前不是"持有即算"，则需要
```
> **存档兼容**：全部走"缺省 = 0 / 空 List → 惰性初始化"（与 新鲜/生长/净化分钟 同一手法，**无需升档版本**，见 开发注意事项 #5）。

### 4.4 `网格实体类型` 增枚举

```csharp
public enum 网格实体类型 { 墙, 容器, 尸体, 敌人, 玩家, 门, 建筑, 障碍, 楼梯, 界外,
                           区域,        // 新：大世界上的区域副本占地
                           临时建筑 }   // 新：搜索生成的临时建筑
```

---

## 五、分刀计划（每刀独立可编译、可验证、可回退）

| 刀 | 内容 | 触碰 | 验证 |
|---|---|---|---|
| **1** | **基类开关 + 图层虚拟化**（纯表现/基类，不改任何玩法） | `网格面板基类`（新增 2 个虚属性）、`探索图层`（格子池） | ① `编译校验`；② **房间/区域逐行行为不变**（房间验证 1800 / 区域验证 400 仍全绿，且试玩房间/区域手感一致）；③ 拿 100×100 空网格冒烟：图数 ≈1,400、帧率正常 |
| **2** | **大世界生成器**（Domain）+ `世界.json` + `DataService` 加载与校验 | `Domain/大世界/大世界生成器.cs`（新）、`数据模型.cs`、`DataService.cs` | 新增 **`Tools/大世界验证`**：逐种子断言（区域占地不重叠 / 不压营地 / 可达 / 连通分量=1 / 障碍撒完成立 / 同种子同布局） |
| **3** | **大世界探索服务**（Service）+ 退役 `地图服务` | `Services/大世界探索服务.cs`（新）、`GameBootstrap.cs`、`地图服务.cs`（删）、`地图地点`（删） | `编译校验`；`Tools/大世界验证` 加"服务级"断言（进/出/走格/时间推进 5 分钟） |
| **4** | **大世界面板 / 图层 / 贴图** + 面板管理器接线（**修掉前端死路**） | `UI/探索/大世界面板|大世界网格面板|大世界图层|大世界贴图.cs`（新）、`面板管理器.cs`（订阅 `打开大地图事件`）、`快速测试面板.cs` | 试玩：主菜单 → 新游戏 → 进大世界 → 走动揭迷雾 → 走到区域进副本 |
| **5** | **区域副本化**：固定种子 + 解锁条件 + 重置计时 + 两把钥匙 | `副本管理器.cs`（新）、`区域探索服务.cs`、`格子探索服务.cs`（虚属性）、`世界时间管理器.cs`（推进计时）、`玩家档案.cs` | `编译校验`；新增断言：同存档同区域布局逐字节一致 / 代次 +1 后内容全新而布局不变 |
| **6** | **大世界搜索 → 临时建筑** | `大世界探索服务.cs`、`搜索服务.cs`、数据 | `编译校验`；试玩：搜索扣行动点/时间、半径内出建筑、进去探索、出来后消失 |
| **7** | **清奇幻残留** | 见 §6 | 逐条：删完 `编译校验` + 三个验证器全绿 |

**刀的先后不可换**：1 是纯表现层前置（不然后面大世界一建就卡）；2/3 是无 UI 的纯逻辑（可离线验证）；4 才让人能看见；5/6 是玩法；7 独立可随时做。

---

## 六、要清的奇幻残留（逐行取证，2026-09-12 盘点）

### 6.1 ★ 先纠正移交报告的一处错误

移交报告 §开发注意事项 #15 与 §2 都写「**技能书数据依赖 `skills.json`（当前缺失，技能字典空）**」——
**这是错的**。实测：`skills.json` **存在**，332 行、21 个技能（重击…绷带包扎），技能字典非空。
真正悬空的是**别的文件引用它时的标识写错了**（见 6.3）。

### 6.2 7 个数据文件不存在，但 `DataService` 照常去加载

`DataService.cs:52/56/57/58/59/66/69` 分别加载 `story` / `quests` / `map` / `facilities` / `training` / `affixes` / `天气`，
而 `Assets/Resources/Data/` 下**这 7 个文件一个都没有** → `数据.剧情 / 任务 / 地图 / 设施 / 训练项目 / 词缀 / 天气` **全是空字典**。

连锁后果（这是"前端死路"的**另一半**根因，报告只写了 `打开大地图事件` 那一半）：

| 后果 | 证据 |
|---|---|
| `DialogueService.进入节点()` 必报「节点不存在」 | 剧情字典空 |
| `数据.天气` **全仓库无人读取**（天气实际靠 `世界时间管理器.cs:361-362` 直接随机取枚举） | 死数据 |
| `任务` / `设施` / `词缀` / `训练` 系统全部空转 | 字典空 |

**处置**：`map` 随 `地图服务` 一起退役（改由 `世界.json` 取代）；其余 6 个按 §6.4 决定"补数据 / 砍系统"。

### 6.3 数据引用悬空（不是文件缺失，是标识写错）

| 悬空项 | 位置 | 后果 |
|---|---|---|
| `职业.json` 的 `初始技能`：近战精通 / 制作 / 侦察 | 不在 `skills.json` | `PlayerService.cs:108` `TryGetValue` **静默跳过** → 职业初始技能不生效 |
| `职业.json` 的 `天赋`：战术本能 / 铁骨 / 美食家 / 职业嗅觉 / 学霸 / 职业操守 | **全部不在** `天赋.json`（该文件只有 强壮…慢性病 20 条） | `PlayerService.cs:122-123` 与 `角色创建面板.cs:365-366` **都不校验**，直接塞进 `档案.天赋` → 职业天赋不生效。其中「美食家」能被 `天赋服务.cs:87` 命中（厨师），「钢铁意志」则 `天赋服务.cs:57` 是**死分支**（无任何来源） |

### 6.4 残留清单

| 残留 | 位置 | 类型 | 处置 |
|---|---|---|---|
| 一整段别名属性（把末日字段伪装成奇幻名） | `玩家档案.cs:409-424`（`体力`/`智力`/`魔力`/`精力`/`物理伤害`/`远程伤害`/`最大魔力`/`最大精力`/`速度加成`/`命中加成`/`饥饿`/`口渴`/`感染度`/`士气`/`噪音值`/`铜币`） | 在用兼容 | 查引用者后删；在用的改为直接用末日字段 |
| `主线阶段` | `玩家档案.cs:240`（注释自承"末日不使用，保留字段防断"）+ `Domain/主线阶段.cs` 整个文件 | 在用兼容 | 删（区域解锁改用 §4.1 的 `解锁` 条件，不需要"主线阶段"） |
| `当前节点` | `玩家档案.cs:239` | 死字段 | 随 `地图服务` 一起删 |
| `地图地点` / `地图根` | `数据模型.cs:434-450` | 死类型 | 删（由 `世界.json` 的大世界数据取代） |
| `对话面板` 结局正文 | `UI/功能面板/对话面板.cs:113`「灰烬镇的余烬还在燃烧。龙在山的深处沉睡。」 | 文案 | 改末日正文 |
| `enemies.json` 的 `魔防` 字段（9 处） | 敌人数据模型（`数据模型.cs:95-119`）**没有该字段** | 死数据 | `JsonUtility` 静默丢弃 → 从 JSON 删掉，免得后人以为它生效 |
| **`设施` 子系统**（交易站 / 诊所 / 家 / 训练场） | `设施工厂.创建()` **0 调用**；`打开功能面板事件` 只被订阅（`面板管理器.cs:137`）**从未被发布**；`打开设施事件` 被 `DialogueService.cs:85` 发布但**无人订阅**；`设施功能面板基类`（`UI/探索/设施功能面板基类.cs:9`）**没有任何子类** | **已末日化、逻辑可用、入口全断** | ⚠ **不是纯奇幻残留** —— 代码质量可留。建议：**接进安全屋**（营地设施）而不是删。要不要接，等大世界跑起来再单独拍板 |
| `skills.json` | **存在且非空**（332 行 / 21 技能） | — | **不用删**；要修的是 6.3 的悬空引用 |
| `story` / `quests` / `facilities` / `training` / `affixes` / `天气` 六个系统 | 数据文件不存在 → 字典全空 | 空转 | **逐个拍板**：补数据 or 砍系统。`天气` 已确认是死数据（无人读取） |

---

## 七、明确不做（别以为漏了）

| 不做 | 为什么 / 什么时候 |
|---|---|
| 大世界上的**视线遮挡** | 与房间/区域一致：方形视野不遮挡（楼会把自己占地中间几格永久遮住 → 楼名被黑雾埋掉，要开得连"楼名画在雾之上"一起做） |
| 大世界上的**街上遭遇** | 报告「准备做」第 6 项：要先把"遭遇与战斗收尾"从 `房间探索服务` **上移到 `格子探索服务`**（两处共用）。这一刀排在 5/6 之后，或与 6 合并 |
| **快速旅行**（已解锁区域间直接跳） | 等解锁条件落地、跑起来手感之后再定 |
| 大世界的**昼夜/天气表**现 | 视野已经随时段切（`视野.json`），天色表现以后再说 |
| `区域出口格`（走上去回大世界） | 现在区域靠 Esc/侧边栏出。副本化之后**必须补**（否则从副本回大世界不自然）→ 归入第 5 刀 |

---

## 八、验证方案（离线，不打开 Unity）

| 工具 | 覆盖 |
|---|---|
| `Tools/编译校验.ps1` | 每刀必跑（`-含Editor` 若动了 Editor） |
| `Tools/房间验证`（现 1800 房间） | 刀的 1/5 **不得让它变化**（布局规则一个都没动） |
| `Tools/区域验证`（现 400 区域） | 刀的 1/2 **不得让它变化**（区域生成规则没动） |
| `Tools/建筑验证`（现 800 楼层） | 同上 |
| **`Tools/大世界验证`（新）** | 100×100 逐种子：区域占地不重叠/不压营地/可达、连通分量 = 1、障碍撒完仍连通、同种子同布局、搜索落点不重叠 |
| **`Tools/副本验证`（新，可并入上一个）** | 两把钥匙：代次 +1 后"布局逐字节一致"而"内容键全部变化" |

**基线（提交 `7535b4e` 实测）**：编译 exit 0 · 区域 400/0 失败 · 建筑 800/0 失败 · 房间 1800/0 失败。

---

## 九、刀2 落地记录（大世界生成器 + 数据 + 验证器）

> 这一刀**纯新增**：新文件 + 新数据 + 新验证器，一行都没碰现在能玩的东西（`地图服务` 还在，刀3 才退役）。

### 9.1 新增/改动

| 文件 | 性质 | 作用 |
|---|---|---|
| `Assets/Scripts/Domain/大世界/大世界生成器.cs` | **新** | 造壳（边界墙）→ 摆营地 → 摆区域 → 撒障碍 + `校验`；`营地门口格` / `区域列表` / `区域门口格` |
| `Assets/Resources/Data/世界.json` | **新** | 废城 100×100；营地 (50,50)；西区（矩形 7×5）/ 东区（L 7×5，解锁：天数 ≥3）；障碍 200；搜索参数 |
| `Tools/大世界验证/`（3 文件） | **新** | 离线验证器：逐种子断言 + 降采样 ASCII 地图 + 区域占地自检 |
| `Assets/Scripts/Data/数据模型.cs` | 追加 | `世界模板` / `世界坐标` / `世界区域项` / `解锁条件项` / `世界搜索参数` / `世界根` + `世界规格`（常量放 Data 层，避免 DataService 反向依赖 Domain）；`区域模板` 增 `重置小时` / `稀有度` |
| `Assets/Scripts/Data/DataService.cs` | 追加 | `加载("世界", …)` + 一段数据级校验（尺寸 / 营地界内 / 区域模板存在 / 占地形状认得出 + 界内 / 解锁条件 / 棋盘 / 搜索参数） |
| `Assets/Scripts/Domain/网格/网格数据.cs` | 追加 | `网格实体类型` 增 `区域` / `临时建筑`（**追加在末尾**，不插中间）；`网格实体.解锁`；`网格数据.区域摆不下` / `撤掉的障碍`；`占位()` 的门口格豁免**扩到 区域** |
| `Assets/Scripts/Domain/区域/建筑外形.cs` | 追加 + 抽取 | 新增 `底边中点(掩, 宽, 高)`（门口格取法）；`区域生成器.选门口格` 改为调它 —— **同一条规则不再写两遍** |
| `Assets/Scripts/Domain/区域/区域生成器.cs` | 重构 | 只把 `选门口格` 的 20 行换成一行调用，**行为必须不变**（用 `房间验证`/`区域验证` 的逐行指纹证明） |

### 9.2 ★ 两个设计决定（值得记下来的）

**① 区域 = 占格 `true` + 只有门口那一格能走**（不是"整片能踩过去"）
最初想的是"区域是一块能踩上去的街区，踩到任意一格就进副本"。否决理由：**那会让"路过"等于"进入"** ——
100×100 的城里横穿一片街区就被拖进副本，玩家没法单纯赶路。
改成与"楼门长在楼上"同一套直觉：整块挡路、**门口那一格**能站能走 → 走上去进副本。
代价是 `网格数据.占位()` 的门口格豁免要从"只认建筑"扩到"建筑 + 区域"（一行）。

**② 门口格的取法上移到 `建筑外形.底边中点`**
区域层的楼门、大世界层的区域门，用的都是"掩码最靠下那一行里最长连续段的中间那格"。
原本这段逻辑私有在 `区域生成器` 里，大世界再抄一遍就是"同一条规则写两遍"（项目已经吃过这个亏）。
抽到 `建筑外形` 后两处共用；`掩码 == null` 按纯矩形处理，与原 `覆盖()` 写法**逐字等价**。

### 9.3 ★ 验证器抓出来的真 bug：区域门口被障碍堵死

**症状**：`大世界验证 50` 报平均 **撤回 3.1 / 世界**（障碍 200 条清单只撒下 196.9 条）。

**推理**：障碍被强制"彼此切比雪夫距离 ≥ 2"，而 **4 连通网格的割集必须是一条 8 连通屏障**（对偶关系）——
障碍之间永远 8 不相邻，所以**障碍在数学上不可能切断开阔地**。那"连通被破坏"就必有别的来源。

**根因**：`可撒障碍` 漏了一条留空规则。区域门口格长在区域占地的**底边**上，左/右/上三面都是区域实体（挡路），
**唯一出路是它下面那一格**。那格一旦落一个障碍，门口格就成了"能站却走不到"的孤岛 → 连通分量 +1。
（`区域生成器.可撒障碍` 早就为"楼门四邻"写了这条；大世界这边漏了。）
而回滚循环是从**最后撒的**开始撤，所以一个堵死的门口会让它白撤好几个才找到真凶 → 平均 3.1。

**修法**：`可撒障碍` 增"不压区域门口四邻"。

**结果**：`撤回 0.0 / 障碍 200.0 / 失败 0`（50 种子）。

**顺带加固的断言**（原来抓不到这个 bug，因为它只断言了"可通行"）：
1. 区域门口格必须**从入口可达**（`可达.Contains`），不只是"可通行"；
2. **`撤掉的障碍` 必须为 0** —— 连通校验只是"万一"的保险，触发即代表真 bug，直接判失败。

> 教训写在 `可撒障碍` 的注释里了：**"可通行" ≠ "走得到"**。凡是"三面被围、只有一个出口"的格子（门口、楼梯口、门内侧），
> 留空规则必须保护它**和它的出口**，断言也必须查**可达性**而不是可通行性。

### 9.4 验证证据（提交前实测）

| 验证 | 结果 |
|---|---|
| `Tools/编译校验.ps1` | 游戏程序集 **exit 0**（145 个源文件，+1） |
| `Tools/大世界验证 50` | **0 失败**；平均区域 2.0 / 障碍 200.0 / **撤回 0.0** |
| `Tools/区域验证 200` | **400 区域 / 失败 0**（与基线一致 → `建筑外形.底边中点` 抽取无行为变化） |
| `Tools/建筑验证 100` | **800 楼层 / 失败 0**（与基线一致） |
| `Tools/房间验证 200` | **1800 房间 / 失败 0，平均容器 5.0 / 敌人 1.9 / 门 1.9**（**与基线逐字一致**，证明重构零影响） |

### 9.5 这一刀**没做**的（不是漏了）

- 大世界里的**遭遇**（街上撞敌人）：要先把"遭遇与战斗收尾"从 `房间探索服务` 上移到 `格子探索服务`（报告「准备做」第 6 项）
- 区域的**临时建筑**生成（搜索那条）：第 6 刀
- **5 个主题区域**（警察局区 / 医院区 / 工厂区 / 居民区 / 商业区）：`世界.json` 现在只引用了**真实存在的** `西区` / `东区`
  —— 主题区域要各自的 `建筑模板` + `房间模板`（警察局/医院/工厂这些楼型还不存在），
  属**内容生产**，排在机制跑通之后，避免先立一堆指向空模板的名字。
- `区域模板.json` 的 `重置小时` / `稀有度` 现在还是缺省 0 / 1（字段已就位，等副本那刀填真值）
