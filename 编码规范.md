# 《最后87天》项目编码规范

> 全局代码书写习惯（所有新代码 + 后续优化统一遵守；发现违反顺手修正）。

## 一、命名

- **全中文标识符**：类名 / 方法名 / 变量名 / 字段名 一律中文（如 `网格面板`、`开始拖拽`、`当前标识`），英文仅限框架类型（`Transform`、`EventBus` 等）。
- **无 namespace**：全部类全局平铺（项目约定，保持现状）。
- 注释中文；方法/类前用 `//` 注释说明职责与关键点。

## 二、语句格式

- **if / else 不写单行**：

  ```csharp
  // ❌ 旧写法
  if (框.数量 == null) return;
  if (条件) { 语句A; 语句B; }

  // ✅ 规范写法
  if (框.数量 == null)
      return;
  if (条件)
  {
      语句A;
      语句B;
  }
  ```

- 规则：
  - 条件后**换行**（不写 `if (x) 语句;` 单行）。
  - **单语句**：不带大括号，**缩进 1 个 Tab**（`if (条件)\n\t语句;`）。
  - **多语句**：带大括号，每行一条语句。
  - `else / else if` 与 `}` 同行（标准 C# 风格）。
- 三元表达式（`?:`）不受此限（单行可读性好，如 `return 条件 ? A : B;`）。
- 方法体每行一条语句；`return` 单独成行。

## 三、结构

- **四层架构**：UI ← 事件 ← 应用层(Services) ← 领域层(Domain，纯 C#) ← 数据层(JSON)。
  - 新逻辑按层归属：UI 只管显示交互；规则/校验进 Domain；流程/服务进 Services。
- **网格面板 partial 分部**：主类 / 渲染 / 拖拽 / 交互 四文件，新功能按职责放对应分部。
- **动态 UI 一律走 `UI工具`**（`UI/框架/UI工具.cs`）：
  - 创建物体/图/文本用 `UI工具.创建<T>`、`创建图`、`创建文本`、`创建居中图`。
  - 锚点定位用 `UI工具.设锚`（单点锚+位置+尺寸一条龙）、`设锚点`（只设锚）、`铺满`（stretch）。
  - **禁止** 手写 `new GameObject + SetParent + anchorMin/anchorMax/pivot/anchoredPosition/sizeDelta` 样板。
- **配色一律走 `网格面板配色`**（颜色/线宽/边距集中一处），禁止散落魔法颜色。

## 四、其他

- 私有方法用 `private`；跨面板共享用 `public`/`[NonSerialized] public`（注明注入方）。
- 场景/预制体引用的 `[SerializeField]` 字段带中文注释（拖什么、谁注入）。
- 协程防御：操作可能销毁的 UI 对象前判空（Unity 假 null）。
- 中文 JSON 字段名与 C# 字段一一对应（JsonUtility）。
