using UnityEngine;

// 种植箱面板：种植箱 家具 的 播种 面板 —— 就地转化面板基类 的 "种植" 版。
// 内容 全 在 基类（网格绑定 / 进度 / 倒计时 / 状态 / 订阅 / 0.5s 轮询）；本类 只 报 预制体 路径，
// 文案 用 基类 默认 那套（生长中… / 可以收获了 / 等待播种 / 剩 hh:mm 成熟）。
// 网格 3×3（家具数据.容器列/行）：单株（容器 非空 就 拒绝 再 放）+ 定根（生长中 不可 拖走）——由 基类 按 功能类型 设。
// 场景搭建：Prefab/种植箱面板（本组件 + 网格 对象 挂 就地转化网格 + 进度 滑条/文本 + 标题/关闭）。
public sealed class 种植箱面板 : 就地转化面板基类
{
    private const string 预制体路径常量 = "Prefab/种植箱面板";   // Resources 路径（预制体 静态 搭建）

    protected override string 预制体路径 => 预制体路径常量;

    // 静态 工厂：家具 右键 打开 种植箱 → 种植箱面板.创建
    public static 种植箱面板 创建(RectTransform 挂载父, 物品堆叠 种植箱)
        => 创建<种植箱面板>(挂载父, 种植箱, 预制体路径常量);
}
