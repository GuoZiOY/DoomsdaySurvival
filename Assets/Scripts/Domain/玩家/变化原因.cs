// 背包变化原因：物品进出背包的**语义**（获得/失去/消耗/出售/购买）。
// 为什么在 Domain 而不是事件层（刀30 从 Events\事件定义.cs 搬来）：
//   它是**领域概念**（"为什么这件东西多了/少了"由领域规则决定），事件只是把它捎带出去。
//   原来放在事件层，导致 Domain 的效果结算 必须引用 Events（反向依赖，`Tools/分层核对.ps1` 报过）。
//   分层是"只能引用本层与更下层"，事件层能引用领域层 ✓，所以枚举放这里后两个方向都合法。
public enum 变化原因 { 获得, 失去, 消耗, 出售, 购买 }
