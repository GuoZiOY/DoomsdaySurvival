# 《最后87天》工作移交报告

> 版本：v35（`705d2d2`）｜ Unity 6 ｜ 末日文字丧尸生存 RPG（塔科夫式背包）
> 报告日期：2026-09-01 ｜ 提交历史：70 次提交（v1~v35）

---

## 一、项目结构

### 1. 代码分层（四层架构，UI ← 事件 ← Services ← Domain ← Data）

```
Assets/Scripts/
├── Core/          组合根与基础设施（3 脚本）
│   ├── GameBootstrap.cs    装配根：注册全部服务 + 挂载时间驱动（幂等，数据校验失败停装）
│   ├── ServiceRegistry.cs  服务注册表（Get<T>/Register）
│   └── EventBus.cs         事件总线（订阅/发布）
├── Data/          数据模型与加载（3 脚本）
│   ├── 数据模型.cs         全部数据类（物品/配方/家具/职业/剧情/地图…）
│   ├── DataService.cs      Resources.Load 加载 JSON + 交叉校验（配方/家具/搜索容器）
│   └── 数据解析.cs         JsonUtility 兼容（中文枚举名 → 字符串）
├── Domain/        领域逻辑（14 脚本，纯 C# 零 UnityEngine 依赖）
│   ├── 玩家档案.cs         数据 + 子管理器装配 + 门面转发
│   ├── 生存管理器.cs       状态增减/睡觉/派生数值（时间结算已迁管理器）
│   ├── 网格服务.cs         塔科夫网格算法（放置/换位/区域交换/堆叠/容器形状块）
│   ├── 容器服务.cs         嵌套容器（弹挂/腰封/背包/弹药箱…）+ 允许类型
│   ├── 持有管理器.cs       统一持有（3 穿戴容器+仓库+嵌套容器递归）
│   ├── 装备管理器.cs       8 槽装备/词缀/穿戴容器
│   ├── 效果结算.cs         剧情/物品效果 → 玩家状态 + 事件发布
│   ├── 战斗单位/成长管理器/任务管理器/品质/五行/主线阶段/货币工具
├── Services/      业务服务（19 脚本）
│   ├── 世界时间管理器.cs   ★ 时间中枢：推进/结算/跳时/时钟同步/事件发布（内嵌驱动）
│   ├── PlayerService.cs    新游戏/读档/解析器接线/初始状态发布
│   ├── 工作台制作服务.cs   制作（校验/扣费/结算/动画上下文）
│   ├── 安全屋管理器.cs     家具建造/升级/修复/拆墙/收音机
│   ├── 安全屋户型.cs       程序化随机户型（BSP 切块 + 墙可拆）
│   ├── 探索服务/搜索服务/地图服务/战斗服务/对话/任务/技能/天赋/词缀/音效/图标/存档/自动存档
├── Events/        事件定义（5 脚本：事件定义/剧情/导航/战斗/探索）
├── UI/            面板层（60 脚本，全场景手动搭建 + 引用位）
│   ├── 框架/       面板基类/面板管理器/UI工具/游戏主题/背景模糊/玩家输入/悬停反馈
│   ├── 背包/       网格面板基类（4 partial）+ 物品/家具 子类 + 制作/建造/安全屋/持有/仓库…
│   ├── HUD/        状态栏 + 侧边栏 + 日志
│   ├── 战斗/探索/地图/角色/主菜单/功能面板/浮层
└── Editor/        地图设计器工具
```

### 2. 数据文件（`Assets/Resources/Data/`，全 JSON 驱动）

```
Data/
├── 物品/
│   ├── items_武器.json       56（近战44+远程12）
│   ├── items_饮食.json       24　items_医疗.json  18
│   ├── items_弹药.json       7　 items_容器.json  3（弹药箱/医药箱/工具箱）
│   ├── items_任务品.json     3
│   ├── 防具/                8 部位文件（头/胸/腿/脚/手/弹挂/腰封/背包）
│   └── 材料/                12 种类文件（医疗用品/建筑/日常/工具/燃料/能源/电子/贵重/情报/食物原料/图纸/其他）
├── recipes_工作台.json       14 条　recipes_灶台.json  7 条　recipes_医疗站.json  12 条
├── 家具.json                8 种（床/工作台/储物箱/收音机/灶台/门/厕所/医疗站）
├── 职业.json / 搜索_地图类型.json / 敌人/剧情/地图/技能/任务/天赋…
└── 物品图标/                图标文件 = 物品标识（约定）
```

**物品总数 256**：武器56 + 防具49 + 饮食24 + 医疗18 + 材料86 + 弹药7 + 容器3 + 任务品3。

---

## 二、功能系统

### 已实现（v1~v35）

| 系统 | 说明 |
|---|---|
| **HUD 状态栏** | 常驻顶栏 6 项：生命(红)/精力(金)/饱食(绿)/水分(蓝)/时间(灰蓝)/地点(灰)。数字去标签，饱食/水分一位小数。事件驱动 + 轮询兜底双保险实时刷新 |
| **世界时间管理器** | 统一时间中枢：挂机推进（1现实分=30游戏分，一天48现实分）、整点结算、制作跳时、睡觉跳时、跨天随机天气、时钟同步防重复结算 |
| **生存结算** | 每游戏时基础消耗 饱食2/水分2；伤病持续/疲劳恢复/寒潮感冒（灶台豁免）；余数按比例折算 |
| **塔科夫式网格背包** | 3 穿戴容器（弹挂/腰封/背包）+ 仓库 + 嵌套容器；物品拖拽/旋转/换位/区域交换/堆叠/存入容器；容器形状块（口袋 1×2/2×2），块偏移上下/左右缝隙，**落点投影格子化**（逐格贴合口袋） |
| **物品体系** | 九大类型 + 二级分类；字段三合一（标识=显示名=图标引用）；按 类型/部位/种类 一文件一组织 |
| **装备系统** | 8 槽位（主手/副手/头/胸/腿/脚/手/背包）+ 穿戴容器；品质/词缀（掉落生成）；耐久 |
| **安全屋** | 程序化随机户型（7 房间，种子存档恢复）；家具建造/升级/修复（0级破损=效率减半）/拆除；拆墙打通房间；房间网格复用网格算法 |
| **制作系统** | 工作台/灶台/医疗站 33 配方；消耗倍率（0.5倍数）；制作动画（进度条+时间流逝+HUD同步，时长=时间分/10秒）；饱食/水分消耗 float 精确 0.1；0级破损家具耗时×2 |
| **生存循环** | 饱食/水分/疲劳/伤病（中毒/感冒/流血/骨折/发烧）/天气（晴/雨/雷雨/寒潮/酷暑）/睡觉推进次日 |
| **探索/搜索** | 区域探索（耗行动点）+ 地图搜索（随机容器生成，箱中箱） |
| **战斗** | 回合制战斗（我方投影/敌人技能/词缀伤害/战败回营地半状态） |
| **存档** | PlayerPrefs JSON 快照（v4）；自动存档（跨天/主线/战斗结束/回主菜单） |

### 核心玩法闭环

```
主菜单 → 角色创建（职业/五维/天赋）
   ↓
营地（安全屋）→ 睡觉/制作/储物/收音机预知天气
   ↓
大地图 → 城镇/荒野
   ↓
探索（耗精力）→ 搜索/战斗/事件 → 物资/图纸/幸存者
   ↓
回营地 → 制作/进食/睡觉 → 推进时间 → 生存结算
   ↓
物资循环 + 时间压力（一天48现实分钟）= 末日生存节奏
```

---

## 三、工作流程（信息链路）

### 1. 状态变化链路（数据 → 事件 → UI）

```
玩家档案（数据源）
   ↓ 修改（生存管理器/世界时间管理器/效果结算…）
EventBus（事件总线）
   ↓ 发布
HUD面板（订阅 6 类事件：生命/魔力/生存状态/时间/地图位置/伤病）
   ↓ 兜底
Update 轮询（对比档案缓存，无事件路径也实时同步）
```

### 2. 时间链路（统一走 世界时间管理器）

```
世界时间管理器（唯一入口）
├── 推进(现实秒)      ← 挂机驱动（每帧）
├── 结算时间段(分,含饱水) ← 整点结算 / 制作补副作用
├── 跳时(游戏分钟)    ← 制作动画
└── 同步整点基准()    ← 外部跳时/取消回滚
        ↓
玩家档案.游戏分钟数 + 饱食度/水分度(float)
        ↓
事件发布 → HUD 刷新
```

### 3. 制作链路（两步 + 动画）

```
点击制作 → 开始制作（校验+扣材料/精力）→ 制作上下文
   ↓
动画（进度条 + 跳时推进时间 + 时钟同步）
   ↓
结算制作（扣饱水按配方 + 补副作用伤病/疲劳 + 产物入包）
   ↓ 背包满 → 原子回滚（材料/精力/时间/饱水全返还）
```

### 4. 服务装配链路（GameBootstrap）

```
EventBus → DataService（校验）→ SaveService → PlayerService（初始化+解析器接线）
→ 世界时间管理器 → 容器/搜索/安全屋/制作/对话/任务/战斗/探索/技能/天赋/词缀/地图
→ 自动存档器 → 挂载时间驱动
```

### 5. 存档链路

```
保存：玩家档案 → JsonUtility → PlayerPrefs(v4 键)
读取：PlayerPrefs → 反序列化 → 接线解析器 → 清理非法值 → 发布初始状态
版本：v3→v4（饱食/水分 float 化，旧档换键失效 → 新游戏重置）
```

---

## 四、数据框架（核心字段约定）

### 物品数据（九大类型）

```
标识(唯一=显示名=图标) / 描述 / 类型(9) / 种类(二级) / 套装(7套) / 槽位
近程远程 / 武器种类 / 攻击/防御/生命/负重/抗性 / 恢复量/恢复目标
形状宽×高 / 重量 / 堆叠上限 / 最大耐久 / 品质
是容器 / 容器列行 / 容器允许类型 / 容器形状(块) / 价值 / 战斗内使用…
```

### 配方数据

```
标识 / 名称 / 描述 / 类型(工作台|灶台|医疗站)
产物 / 产物数量 / 材料[] / 解锁图纸 / 需要等级(1~3)
制作时间分 / 消耗倍率(0.5倍数) / 消耗精力
```

### 家具数据

```
标识 / 名称 / 描述 / 材料[] / 最大等级 / 功能类型(睡觉|制作|仓库|情报|取暖|防护|卫生|医疗)
形状宽高 / 升级[](材料+形状) / 效果[](按等级)
0级=破损（可用但效率减半）；未建=无效果；储物箱例外不缩水
```

### 关键约定

- **图标 = 标识**（`Resources/物品图标/<标识>.png`）
- **网格格尺寸 = 90**；**块缝隙宽 = 10**；容器物品必须落单块内
- **时间速率**：1 现实分 = 30 游戏分；一天 48 现实分
- **基础消耗**：每游戏时 饱食2 / 水分2（float 精确 0.1）
- **制作动画**：时长 = 制作时间分 ÷ 10 现实秒
- **场景搭建**：UI 全场景手动搭建 + `[SerializeField]` 引用位（无代码找路径）

---

## 五、已做 / 在做 / 准备做 / 以后做

### ✅ 已经完成（v1~v35）
1. 四层架构 + 事件总线 + 数据驱动 + 存档（v4）
2. 物品体系重构（九大类型/256件/一类型一文件/字段三合一）
3. 塔科夫式网格（穿戴容器/口袋形状/格子投影/拖拽全套）
4. 安全屋（随机户型/家具建造升级/拆墙/制作三站）
5. 世界时间管理器（时间统一调度）
6. HUD 状态栏（实时刷新）
7. 生存循环（饱食/水分/伤病/天气/睡觉）
8. 探索/搜索/战斗/任务/职业天赋/词缀系统

### 🔄 正在做（安全屋家具功能完善）
- 房间功能化（房间名显示：大厅/厨房/主屋/洗浴间/阳台/二间 差异化加成）
- 睡觉交互改造（右键床睡觉 + 夜间事件——激活 门=防御、厕所=卫生）
- 家具效果完善（0级破损减半已实现，正在做使用交互）

### 📋 准备做（已确认方向）
1. **房间名显示 + 类型功能**（厨房做饭加成/主屋睡觉加成/阳台接水/洗浴间恢复）
2. **睡觉交互 + 夜间事件**（入侵概率随门等级、卫生影响恢复）
3. **电力系统**（材料已备：电池/充电宝/发电机/太阳能板/电线；发电机+电灯家具，收音机改回耗电）
4. **幸存者收留**（探索救回 → 住二间 → 生产/守卫/做饭加成；字段已预留）
5. **新家具**（水箱储水/冰箱保鲜/工具架修耐久/书架研究解锁）

### 🔮 以后做（待设计）
- 主线剧情/结局（《最后87天》主题）
- 城市地图三级导航（城镇→设施→内部）
- 交易系统（以物易物）
- 任务板（主线/支线）
- 更丰富敌人/词缀/品质梯度
- 音效/美术资源完善

---

## 六、开发注意事项（坑与约定）

1. **编码**：全中文命名；JSON/C# 必须 UTF-8 无 BOM；pwsh 读写中文需显式编码
2. **场景**：UI 手动搭建 + 引用位；新增组件挂场景后需全局判重（防双驱动）
3. **数据**：改物品标识 → 同步 配方/图标文件名/快速测试面板/搜索容器引用；json+meta 成对（GUID 固定）
4. **时间**：任何推进时间必须走 世界时间管理器（禁止直接改 游戏分钟数，防结算重复/遗漏）
5. **存档**：结构变动 → 版本+1（当前 v4）；旧档重置策略已确认
6. **网格**：改渲染前先查 块偏移 链路（计算块偏移→格x/格y→定位→投影→边界线）
7. **验证**：Unity 刷新运行验证；数据校验看 GameBootstrap 日志（校验错误停装）
